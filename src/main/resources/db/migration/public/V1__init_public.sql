CREATE TABLE IF NOT EXISTS config_value (
                                            id BIGINT GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START 1 CACHE 1 NO CYCLE) NOT NULL,
    config_group VARCHAR(100) NULL,
    key VARCHAR(100) NOT NULL,
    value VARCHAR(1024) NOT NULL,
    CONSTRAINT pk_config_value PRIMARY KEY (id),
    CONSTRAINT uc_config_value_key UNIQUE (key)
    );